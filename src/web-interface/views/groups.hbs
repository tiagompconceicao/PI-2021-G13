<div class="container">
  <h1>{{title}}</h1>
  <div class="row">
  {{#each groups}}
    <div class="col-sm-3">
      <div class="card" style="width: 18rem;">
        <div class="card-body">
          <h5 class="card-title">{{name}}</h5>

          <p class="card-text">{{description}}</p>
          <a href="#" class="card-link"><a href="groups/{{id}}">Card details</a>
        </div>
      </div>
    </div>
  {{/each}}
  </div>
  
  <a href="groups/new-group">Create a new group</a>
</div>